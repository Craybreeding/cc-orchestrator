interface:
  display_name: "Kimi Model Router"
  short_description: "Safely route non-trivial coding, debugging, and reviews between Kimi and native Codex."
  default_prompt: "Use $kimi-model-router to select a safe model route before a Kimi coding or review call."
policy:
  allow_implicit_invocation: true
